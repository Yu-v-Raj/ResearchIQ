# ─────────────────────────────────────────
# Multi-Agent Research System - Requirements
# ─────────────────────────────────────────

# ── Core LangChain Stack ──
langchain                        # Base framework (tools, chains)
langchain-core                   # Prompts, output parsers, LCEL
langchain-groq                   # ChatGroq LLM wrapper
langgraph                        # Agent orchestration (create_react_agent)

# ── Tools ──
tavily-python                    # Tavily web-search tool
beautifulsoup4                   # HTML scraping (bs4)
requests                         # HTTP requests
rich                             # Pretty terminal output (tools.py)

# ── Frontend ──
streamlit                        # Streamlit UI (app.py)
fastapi                          # REST API (api.py)
uvicorn                          # ASGI server for FastAPI

# ── Environment & Config ──
python-dotenv                    # Load .env API keys

# ── Utilities ──
pydantic                         # Data validation (used by FastAPI & LangChain)
